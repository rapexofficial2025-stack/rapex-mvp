import { EmptyState } from "@rapex/ui-web";

export function OrdersPage() {
  return (
    <div style={{ padding: 24 }}>
      <EmptyState title="Orders" description="Coming soon -- this session's focus was the Store screen." />
    </div>
  );
}
