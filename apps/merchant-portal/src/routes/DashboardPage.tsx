import { EmptyState } from "@rapex/ui-web";

export function DashboardPage() {
  return (
    <div style={{ padding: 24 }}>
      <EmptyState title="Dashboard" description="Coming soon -- this session's focus was the Store screen." />
    </div>
  );
}
