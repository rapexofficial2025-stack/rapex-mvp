# utils/

App-specific helpers only. Cross-app pure functions (validation, date, currency, location, image, role/permission helpers) live in [`@rapex/utils`](../../../packages/utils) instead — check there first before adding something here.
