# contexts/

App-wide React contexts (Auth, Theme, Location, Notification, Wallet, Cart, Wishlist) — arrives in Sprint FE-06. `providers/AppProviders.tsx` is where each context provider gets composed once built.
